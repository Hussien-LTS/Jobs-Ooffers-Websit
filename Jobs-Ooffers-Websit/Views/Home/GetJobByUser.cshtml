@model IEnumerable<Jobs_Ooffers_Websit.Models.ApplyForJob>
@{
    ViewBag.Title = "GetJobByUser";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>الوظائف المتقدم اليها </h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.job.JobTitle)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Msg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApplyDate)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelIthem => item.job.JobTitle)
            </td>
            <td>
                @Html.DisplayFor(modelIthem => item.Msg)
            </td>
            <td>
                @Html.DisplayFor(modelIthem => item.ApplyDate)
            </td>
            <td>
                @Html.ActionLink("تعديل", "Edit", new { id = item.Id }) |
                @Html.ActionLink("التفاصيل", "DetailsOfJob", new { id = item.Id })|
                @Html.ActionLink("حذف", "Delete", new { id = item.Id })
            </td>
        </tr>

    }
</table>

